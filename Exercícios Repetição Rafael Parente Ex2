#include <stdio.h>

int main() {

    int n, i;
    
    printf("Informe um numero inteiro positivo: ");
    scanf("%d", &n);
    
    for (i = n; i >= 0; i--) {
        printf("%d\n", i);
    }
    return 0;
}
